sudo chmod 775 /usr/local/bin/mikroa

dependencies that need to be installed

By language:

1) Javascript

  Nothing

2) Typescript

  -D
  @types/node
  eslint-config-avilatek-typescript
  typescript
  Every @types in case the other dependencies need it

By orm

1) Mongoose

  mongoose

  -D
  @types/connect-mongo
  @types/mongoose

2) Sequelize

  sequelize


3) Prisma

By framework

1) Express

  express

2) Fastify

  fastify
  nodemon
  fastify-plugin

3) Koa.js





A) mongoose 

https://betterprogramming.pub/how-to-use-mongoose-with-node-js-913a8073b29c

1. npm install mongoose --save
   npm i dotenv

2. Initialize the database

```
const mongoose = require('mongoose');

async function main() {
  mongoose
    .connect(String(process.env.DATABASE), {
      useNewUrlParser: true,
      useUnifiedTopology: true,
      useFindAndModify: false,
      useCreateIndex: true,
    })
    .then(() => {
      console.log(`🤩🍃  MongoDB is running`);
    })
    .catch((err) => {
      console.log(`❌🤬 ${err}`);
      process.exit();
    });

  mongoose.connection.on('error', (err) => `❌🤬❌🤬 ${err}`);

  app.listen({ port: PORT }, () =>
    console.log(
      `🚀 Server ready at http://localhost:${PORT}`
    )
  );
}

main();
```